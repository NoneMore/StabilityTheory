# Next Phase TODO

This file isolates and refines the next stage from `PLAN.md`.

## Phase 1: Stone Space - Compactness of the Type Space

Outcome:
- [ ] `CompleteType T alpha` has a `CompactSpace` instance.
- [ ] `CompleteType T alpha` has a `T2Space` instance.
- [ ] The key supporting lemmas for `typesWith` and `typeOf` are available.
- [ ] The new topology file builds cleanly with the rest of the project.

Why this phase matters:
Mathlib already provides the totally separated side. The missing core step is to prove
`CompactSpace (CompleteType T alpha)`, which unlocks the Stone-space package needed
for the later Cantor-Bendixson and Morley-rank development.

## File Setup

- [ ] Create `StabilityTheory/ModelTheory/Topology/Types.lean`.
- [ ] Choose the minimal import list from Mathlib topology/model theory files and the
      local `Types` development.
- [ ] Decide whether any umbrella file also needs an import for the new module.
- [ ] Keep statements and names aligned with surrounding Mathlib style.

## Context Audit

- [ ] Inspect the existing topology already available on `CompleteType T alpha`.
- [ ] Locate the exact Mathlib theorem/API names needed for:
      topology generated by `range typesWith`,
      Alexander's subbasis theorem,
      first-order compactness,
      separation lemmas used for `T2Space`.
- [ ] Record any bridge lemmas already present for `typesWith`, `typeOf`, or clopen sets.
- [ ] Confirm whether `T2Space` can be inferred directly once compactness is available.

## Task 1: Compactness of the Type Space

- [ ] Add the target instance:

```lean
instance : CompactSpace (CompleteType T alpha)
```

- [ ] Formalize the subbasis used for the topology on `CompleteType T alpha`.
- [ ] State or prove the helper lemma that translates a cover by `typesWith` sets
      into inconsistency of `T U { not phi_i }`.
- [ ] Use first-order compactness to extract finite inconsistency.
- [ ] Convert finite inconsistency back into a finite subcover.
- [ ] Finish the `CompactSpace` instance with the chosen Alexander-subbasis theorem.
- [ ] Add short docstrings only where the proof structure is not obvious.

## Task 2: Hausdorff Structure

- [ ] Add the target instance:

```lean
instance : T2Space (CompleteType T alpha)
```

- [ ] First try the shortest typeclass-driven proof from existing separation instances
      plus compactness.
- [ ] If direct inference fails, add the minimal explicit proof with no redundant lemmas.
- [ ] Check that the instance does not create typeclass loops or duplicate-instance noise.

## Task 3: Supporting Lemmas

- [ ] Prove `CompleteType.isCompact_typesWith`.
- [ ] Prove `CompleteType.continuous_typeOf`.
- [ ] Add closed-set reformulations for `typesWith`.
- [ ] Add clopen-set reformulations for `typesWith`.
- [ ] Prefer statements that will be reusable in the Cantor-Bendixson and Morley-rank files.

## Integration

- [ ] Make sure the new file is reachable from the project's import tree if needed.
- [ ] Check whether later files should import this new topology module instead of raw Mathlib files.
- [ ] Avoid touching unrelated proof files unless an import edge requires it.

## Verification

- [ ] Build the new file directly if that is supported by the current workflow.
- [ ] Run `lake build`.
- [ ] Resolve any import, instance, or theorem-name regressions.
- [ ] Confirm the repository still compiles cleanly after the new file is added.

## Acceptance Criteria

- [ ] `StabilityTheory/ModelTheory/Topology/Types.lean` exists and compiles.
- [ ] `CompactSpace (CompleteType T alpha)` is available.
- [ ] `T2Space (CompleteType T alpha)` is available.
- [ ] `CompleteType.isCompact_typesWith` is available.
- [ ] `CompleteType.continuous_typeOf` is available.
- [ ] `typesWith` has usable closed/clopen reformulations.
- [ ] Phase 1 milestone is satisfied:
      `CompleteType T alpha` is formally available as a Stone space.
